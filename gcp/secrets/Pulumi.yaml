name: gcp-secrets
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: GCP Infrastructure for Secrets Mgt
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
